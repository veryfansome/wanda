"""wanda's memory: a markdown vault (the truth), a derived SQLite index (a
cache), and the passes that move observations from the fast lane into the
curated hierarchy. See docs in the plan: markdown is truth, the index is
disposable, trust is edges, provenance is assigned by the harness."""
